/**
 * oat - Vue Port
 * Simplified Vue wrappers derived from the oat web components (src/js/).
 * The oat custom elements remain the base layer; these components wrap them.
 *
 * Peer requirements: vue >= 3.0, oat CSS + JS loaded in the page.
 */

export { useOtBase } from './base.js';
export { default as OtTabs } from './OtTabs.vue';
export { default as OtDropdown } from './OtDropdown.vue';
